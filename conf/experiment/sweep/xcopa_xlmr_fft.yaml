# @package _global_

defaults:
  - override /dataset:
    - balanced_copa
    - social_i_qa
  - override /madx: null
  - override /model: xmlr
  - override /params: params
  
dataset:
  balanced_copa:
    train_split: train
    val_split: test
  social_i_qa:
    train_split: train
    val_split: validation
model:
  num_labels: 2
params:
  max_epochs: 2