# @package _global_

defaults:
  - override /dataset:
    - paws_x.yaml
  - override /model: mbert.yaml
  - override /lang_adapter: null
  - override /task_adapter: null
  
dataset:
  paws_x:
    train_split: train
    val_split: validation
    test_split: validation

model:
  num_labels: 2