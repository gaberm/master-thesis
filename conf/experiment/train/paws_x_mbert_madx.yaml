# @package _global_

defaults:
  - override /dataset:
    - paws_x
  - override /exp_name: train/paws_x_mbert_madx
  - override /madx: madx
  - override /model: mbert
  - override /params: params

dataset:
  paws_x:
    train_split: train
    val_split: validation
    test_split: validation
madx:
  task_adapter:
    name: paws-x
    reduction_factor: 4
model:
  num_labels: 2
params:
  lr: 1e-4

