# @package _global_

defaults:
  - override /dataset:
    - paws_x
  - override /exp_name: train_paws_x_xlmr_fft
  - override /madx: null
  - override /model: xmlr
  - override /params: params
  
dataset:
  paws_x:
    train_split: train
    val_split: validation
    test_split: validation
model:
  num_labels: 2
params:
  lr: 2e-5