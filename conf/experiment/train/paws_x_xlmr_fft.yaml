# @package _global_

defaults:
  - override /dataset:
    - paws_x
  - override /madx: null
  - override /model: xmlr
  - override /params: params
  - override /trainer: trainer
  
dataset:
  paws_x:
    train_split: train
    val_split: validation
    test_split: validation
model:
  num_labels: 2
params:
  lr: 2e-5
trainer:
  exp_name: train_paaws_x_xlmr_fft