# @package _global_

defaults:
  - override /dataset:
    - paws_x
  - override /exp_name: train/paws_x_mbert_fft
  - override /madx: null
  - override /model: mbert
  - override /params: params
  
dataset:
  paws_x:
    train_split: train
    val_split: validation
    test_split: validation
model:
  num_labels: 2
lr: 3e-5