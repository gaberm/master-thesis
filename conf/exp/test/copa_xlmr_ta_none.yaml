# @package _global_

defaults:
  - override /dataset: xcopa
  - override /madx: madx
  - override /model: xlmr
  - override /params: params
  - override /trainer: trainer

madx:
  task_adapter:
    name: copa 
model:
  num_labels: 2
  load_ckpt: true
  ckpt_averaging: none
trainer:
  devices: 1
  exp_name: copa_xlmr_ta_none