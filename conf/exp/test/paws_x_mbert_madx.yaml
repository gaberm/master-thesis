# @package _global_

defaults:
  - override /dataset:
    - paws_x
  - override /madx: madx
  - override /model: mbert
  - override /trainer: trainer
  
dataset:
  paws_x:
    train_split: train
    val_split: validation
    test_split: validation
model:
  num_labels: 2
  ckpt_dir: /ceph/mgaber/data/checkpoints/paws_x_mbert_madx/pretty-silence-149
madx:
  task_adapter:
    name: paws-x
trainer:
  exp_name: paws_x_mbert_madx