# @package _global_

defaults:
  - override /dataset:
    - paws_x
  - override /madx: madx
  - override /model: mbert
  - override /trainer: trainer
  
dataset:
  paws_x:
    train_split: train
    val_split: validation
    test_split: validation
model:
  num_labels: 2
  ckpt: /ceph/mgaber/data/checkpoints/paws_x_mbert_madx/epoch=4-accuracy=0.906.ckpt
madx:
  task_adapter:
    name: paws-x
trainer:
  exp_name: paws_x_mbert_madx