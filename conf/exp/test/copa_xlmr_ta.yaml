# @package _global_

defaults:
  - override /dataset: xcopa
  - override /madx: madx
  - override /model: xlmr
  - override /params: params
  - override /trainer: trainer
  
model:
  num_labels: 2
  load_ckpt: true
madx:
  task_adapter:
    name: copa
params:
  batch_size: 24
trainer:
  devices:
    darwin: auto
    linux: 1
  exp_name: copa_xlmr_ta