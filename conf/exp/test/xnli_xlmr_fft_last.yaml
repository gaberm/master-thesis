# @package _global_

defaults:
  - override /madx: null
  - override /model: xlmr
  - override /trainer: trainer

model:
  load_ckpt: true
  ckpt_averaging: last
trainer:
  exp_name: xnli_xlmr_fft_last