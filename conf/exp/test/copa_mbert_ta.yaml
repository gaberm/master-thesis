# @package _global_

defaults:
  - override /dataset: xcopa
  - override /madx: madx
  - override /model: params
  - override /trainer: trainer

model:
  num_labels: 2
  load_ckpt: true
params:
  batch_size: 24
madx:
  task_adapter:
    name: copa
trainer:
  devices:
    darwin: auto
    linux: 1
  exp_name: copa_mbert_ta