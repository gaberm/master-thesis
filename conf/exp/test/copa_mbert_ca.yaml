# @package _global_

defaults:
  - override /dataset: xcopa
  - override /madx: null
  - override /params: params
  - override /trainer: trainer

model:
  num_labels: 2
  load_ckpt: true
  ckpt_averaging: true
  ckpt_dir: checkpoints/copa_mbert_fft/seed_0
params:
  batch_size: 24
trainer:
  devices: 1
  exp_name: copa_mbert_ca
