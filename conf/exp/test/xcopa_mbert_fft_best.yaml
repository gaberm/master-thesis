# @package _global_

defaults:
- override /dataset: xcopa
- override /madx: null
- override /params: params
- override /trainer: trainer

model:
  ckpt_avg: best
  ckpt_dir: xcopa_mbert_fft
  load_ckpt: true
  num_labels: 2
params:
  batch_size: 8
trainer:
  exp_name: xcopa_mbert_fft_best
