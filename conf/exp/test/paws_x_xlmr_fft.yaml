# @package _global_

defaults:
  - override /dataset:
    - paws_x
  - override /madx: null
  - override /model: xlmr
  - override /trainer: trainer
  
dataset:
  paws_x:
    train_split: train
    val_split: validation
    test_split: validation
model:
  num_labels: 2
  ckpt_path: /ceph/mgaber/data/checkpoints/paws_x_xlmr_fft/epoch=4-accuracy=0.925.ckpt
trainer:
  exp_name: paws_x_xlmr_fft