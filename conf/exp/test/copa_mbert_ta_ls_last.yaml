# @package _global_

defaults:
  - override /dataset: xcopa
  - override /madx: madx
  - override /params: params
  - override /trainer: trainer

madx:
  task_adapter:
    name: copa
model:
  num_labels: 2
  load_ckpt: true
  ckpt_avg: last
  ckpt_dir: copa_mbert_ta_ls
trainer:
  devices: 1
  exp_name: copa_mbert_ta_ls_last