# @package _global_

defaults:
  - override /dataset: xcopa
  - override /madx: null
  - override /model: xlmr
  - override /params: params
  - override /trainer: trainer

model:
  num_labels: 2
  load_ckpt: true
  ckpt_avg: none
  ckpt_dir: copa_xlmr_fft_ls
params:
  batch_size: 8
trainer:
  devices: 1
  exp_name: copa_xlmr_fft_ls_none