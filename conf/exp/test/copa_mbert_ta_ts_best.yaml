# @package _global_

defaults:
  - override /dataset: xcopa
  - override /madx: madx
  - override /params: params
  - override /trainer: trainer

madx:
  task_adapter:
    name: copa
model:
  num_labels: 2
  load_ckpt: true
  ckpt_avg: best
  ckpt_dir: copa_mbert_ta
params:
  temp_scaling: True
trainer:
  devices: 1
  exp_name: copa_mbert_ta_ts_best