# @package _global_

defaults:
  - override /madx: null
  - override /model: mbert
  - override /trainer: trainer

model:
  load_ckpt: true
  ckpt_avg: none
  ckpt_dir: xnli_mbert_fft
params:
  temp_scaling: True
trainer:
  exp_name: xnli_mbert_fft_ts_none