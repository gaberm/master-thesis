# @package _global_

defaults:
  - override /dataset:
    - xcopa
  - override /madx: null
  - override /trainer: trainer

model:
  num_labels: 2
  ckpt_dir: /ceph/mgaber/data/checkpoints/xcopa_mbert_fft/
trainer:
  exp_name: xcopa_mbert_fft