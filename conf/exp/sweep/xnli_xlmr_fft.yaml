# @package _global_

defaults:
  - override /dataset: 
    - xnli
  - override /madx: null
  - override /model: xlmr
  - override /params: params

dataset:
  xnli:
    train_split: train
    val_split: validation
    test_split: validation
params:
  max_epochs: 2