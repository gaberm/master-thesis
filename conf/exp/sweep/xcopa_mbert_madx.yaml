# @package _global_

defaults:
  - override /dataset:
    - balanced_copa
    - social_i_qa
  - override /model: mbert
  - override /madx: madx
  - override /params: params

madx:
    task_adapter:
      name: copa
model:
  num_labels: 2
params:
  max_epochs: 2