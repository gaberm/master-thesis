# @package _global_
defaults:
- override /dataset: paws_x
- override /madx: null
- override /model: mbert
- override /params: params
- override /trainer: trainer
dataset:
  target_lang: [de, en, es, fr, ja, ko, zh]
model:
  ca_strategy: none
  ckpt_dir: mslt_en_en_paws_x_mbert_fft
  num_labels: 2
params:
  lr: 2e-5
  source_lang: [en, en]
trainer:
  exp_name: mslt_en_en_paws_x_mbert_fft