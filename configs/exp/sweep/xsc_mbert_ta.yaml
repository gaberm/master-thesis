# @package _global_

defaults:
- override /dataset: xxstorycloze
- override /model: mbert
- override /madx: madx
- override /params: params
- override /trainer: trainer

madx:
  task_adapter:
    name: xstorycloze
model:
  batch_size: 6  
  num_labels: 2
params:
  max_epochs: 2
trainer:
  
