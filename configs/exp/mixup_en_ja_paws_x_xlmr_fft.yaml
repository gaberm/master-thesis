# @package _global_
defaults:
- override /dataset: paws_x
- override /madx: null
- override /model: xlmr
- override /params: params
- override /trainer: trainer
dataset:
  target_lang: [de, es, fr, ko, zh]
model:
  ckpt_avg: none
  ckpt_dir: mixup_en_ja_paws_x_xlmr_fft
  num_labels: 2
params:
  lr: 2e-5
  source_lang: [en, ja]
trainer:
  exp_name: mixup_en_ja_paws_x_xlmr_fft